export function SiteBurdenFootnote() {
  return (
    <p className="footnote">
      Identical ancestry mix at a site yields the same at-risk rate; expected
      counts scale with planned enrolment (N).
    </p>
  );
}
